// 98. Показать треугольник Паскаля.

int i, c, k;
System.Console.WriteLine("Введите нужное количество строк треугольника Паскаля");
int a=System.Convert.ToInt32(Console.ReadLine());

static extern int Factorial(int N);
{
    int m, x = 1, N;
    for (m=1; m<=N; m++)
        x*=m;
    return x;

}